SELECT 
    INFO.G_TITLE || ' - ' || INFO.G_ARTIST,
    COUNT(1) AS TOTAL_PLAYS

FROM 
    LISTEN_HISTORY LH
        INNER JOIN MUSIC_INFO_GOOGLE_ONLY INFO ON
            INFO.SONG_URL = LH.SONG_URL

GROUP BY
    INFO.G_TITLE || ' - ' || INFO.G_ARTIST

ORDER BY COUNT(1) DESC

LIMIT 10;