SELECT 
    ROW_NUMBER() OVER (ORDER BY COUNT(1) DESC) AS Rank,
    LISTEN_DATE, 
    COUNT(1) AS TOTAL_PLAYS
FROM 
    LISTEN_HISTORY
GROUP BY 
    LISTEN_DATE
ORDER BY 
    TOTAL_PLAYS DESC
LIMIT 25;